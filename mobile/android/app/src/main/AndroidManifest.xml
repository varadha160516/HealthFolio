<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The app is entirely network-dependent (talks to the CareLoop backend for every screen).
         Flutter's debug/profile builds get this automatically via src/debug and src/profile
         AndroidManifest.xml overlays for tooling reasons, but release builds don't — it must be
         declared here or the release APK can make no network calls at all. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- For fingerprint/face unlock (local_auth). USE_FINGERPRINT covers API 23-27 devices;
         USE_BIOMETRIC is the modern one (API 28+, BiometricPrompt). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <!-- For "nearby doctors" search (geolocator) — coarse is enough for a 20km radius search,
         but fine is requested too since it's a normal runtime-prompt permission either way. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- For voice input/output in AI Chat (speech_to_text + flutter_tts) — both run on-device,
         no cloud voice service or extra API key needed. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <application
        android:label="CareLoop"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ package-visibility requirement for speech_to_text to find the
             on-device speech recognizer. -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
