/// The eight standard ABO/Rh blood group values — shared between the "Add a dependent" form and
/// the Profile tab's edit-personal-details dialog so both enforce the same valid set rather than
/// accepting free-text.
const kBloodGroups = ['A+', 'A-', 'B+', 'B-', 'AB+', 'AB-', 'O+', 'O-'];
